
########## REVIEW 1 ##########
SUMMARY: Reviewed packages/mandate/**, ops/migrations/**, docs/adr/** and the root package.json scripts against §12, §19 and the §2 principles. Ran `node --test` (32/32 pass), the CLI (`migrate`, `ingest`, `coverage`, `query person`, `resolve`, `audit`), and the real ingest into throwaway databases (repo `.data/` untouched, nothing edited).\n\nWHAT IS CLEAN — P5 is fully conformant, verified by query, not by reading: `legal_case` has 0 rows after the real ingest, the bare `criminalCases` count lands as 2,920 `pending_cases_declared` claims with unit 'cases' cited to the candidate's own affidavit, no predicate anywhere contains 'charged' or 'convict', and the DDL CHECK includes 'unknown'. §19 table/column fidelity is high: all 21 Postgres tables and all 4 ClickHouse tables are present with their NOT NULLs, UNIQUEs, FKs, CHECKs and both specified indexes intact, plus the tables §19 references but never defines (`symbol`, `alliance*`) and §12's `person_identifier`; the only downgraded constraint is `party_version`'s EXCLUDE, documented in ADR 0001. Both ADRs are substantive and honest about divergences. P4 epoch alignment holds: 0 of 1,176 contests are cross-epoch, every contest points at a `delim-2008` place_version matching its election's epoch, and the epoch's `effective_from` (2008-02-19) is a real date with a source. Ingest data integrity is good and no row is silently dropped: 294×4 = 1,176 contests exactly; 2,920 candidates in → 2,936 2026 candidacies out (the 16 extra are declared contestants who matched no nomination, each recorded as an `unmatched_declared_winner` anomaly); 31 unresolved party labels are all recorded as anomalies with counts, synthesised as `registered_unrecognised` parties and preserved verbatim on 108 `candidacy.party_raw` values, with 0 candidacies left without a party_version; the 40 duplicate `(year, constituencyId)` result rows are reported verbatim rather than guessed into a contest; 22,414/22,414 claims are cited and 4,359/4,359 results carry a resolvable source_id. Re-ingesting the *same bytes* is genuinely idempotent (identical counts across two real runs).\n\nWHAT IS NOT — four blockers. (1) `core/index.ts` re-exports a file that does not exist, so the core barrel throws ERR_MODULE_NOT_FOUND. (2) `claim.id` is a per-run counter used as an upsert key: after any input edit, re-running `registry:ingest` leaves 17,536 of 22,414 claims citing a second, unrelated document (one person's assets citing another's affidavit) while the CLI reports `uncited values 0` and exits 0 — P2's central claim becomes false silently. (3) The declared-winner upgrade overwrites `age_declared` and `education_declared` with NULL for 293 of the 294 2026 winners. (4) §12's resolution pipeline does not exist — empty `resolve/` directory, no scoring, no human-queue table, `registry:resolve` and `registry:audit` both exit 1, and ADR 0001 asserts `registry:audit` enforces five cross-table invariants (including P5's convicted⟹court_order) that have no runner. Beyond those: the same sequential-surrogate-id pattern breaks the `party_version` overlap invariant on a shrinking party register (reproduced), and nothing operational ever queries the replacement view; 99.7% of source rows carry a URL-derived `doc_hash` and an asserted-not-observed `retrieved_at`, with 586 citations pointing at a bare site root; and P3 has zero symbol data despite 16 of 32 parties carrying `symbolUrl` in the input.

[blocker / correctness] packages/mandate/src/core/index.ts:5
  CLAIM: The core barrel re-exports `./indic/index.ts`, which does not exist, so importing `src/core/index.ts` throws at module resolution time.
  FAILS: `node -e "await import('./packages/mandate/src/core/index.ts')"` → `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../src/core/indic/index.ts' imported from .../src/core/index.ts`. `src/core/indic/` is an empty directory. The package's documented entry point for core types (README: "src/core/ entity types, IDs, temporal helpers, Indic normalisation") cannot be imported by anything — cycle 2's API/UI code hits this on its first import. The ingest only survives because it bypasses the barrel and imports `../../core/ids.ts` directly (wb-static.ts:21, with a GAP comment at line 24 acknowledging it), and no test imports the barrel, so `npm test` is green.
  FIX:   Either drop line 5 until `core/indic/index.ts` lands, or add the file. A one-line barrel that names a non-existent module is worse than no barrel.

[blocker / provenance-correctness] packages/mandate/src/ingest/sources/wb-static.ts:463
  CLAIM: `claim.id` is a per-run sequence number (`++claimSeq`) used as an upsert key, so any change to the input data re-numbers every claim and leaves the previous run's citation rows attached to the new claims — claims end up citing documents that do not contain the number. P2 becomes false silently.
  FAILS: Verified by running the real ingest twice into one db, the second time with one candidate removed from the front of `candidates.ts` (a scraper re-run or a dedupe fix — exactly the documented `registry:ingest` path, since `--fresh` is optional): claim 1 was `candidacy:...:hiten-barman / affidavit.assets.total` in run 1 and becomes `candidacy:...:kamal-kumar-roy / affidavit.assets.total` in run 2. Because `citation` upserts on (claim_id, source_id, page_no), the old row survives beside the new one: **17,536 of 22,414 claims now carry two citations**, one of them another person's affidavit (`SELECT c.id, s.title ...` → claim 1 cites both "Affidavit — Kamal Kumar Roy (c0001)" and "Affidavit — Hiten Barman (c0001)"). `countUncited()` only looks for *missing* citations, so `mandate ingest` reports `uncited values 0` and exits 0. Clicking through to the source for Kamal Kumar Roy's assets shows Hiten Barman's affidavit.
  FIX:   Content-derive the claim id the way source ids already are: `contentId([subjectRef, predicate, asOf ?? ""])` (and make `citation` upsert on (claim_id, source_id, page_no) as now — a content id makes that key stable). Alternatively delete claims+citations for the run's subject_refs before writing. Add a test that re-ingests a *mutated* bundle, not just the same bytes.

[blocker / data-loss] packages/mandate/src/ingest/sources/wb-static.ts:889
  CLAIM: The "declared winner" upgrade re-pushes the candidacy row with `age_declared` and `education_declared` hard-coded to null, and the upsert's `DO UPDATE SET` overwrites the affidavit-derived values with those nulls.
  FAILS: Real ingest: `SELECT status, COUNT(*), SUM(age_declared IS NULL), SUM(education_declared IS NULL) FROM candidacy WHERE contest_id LIKE 'wb-assembly-2026:%' GROUP BY status` → `contesting 2643 / 0 / 0` but `elected 293 / 293 / 293`. Every one of the 293 matched 2026 winners loses the age and education their affidavit declared (e.g. `wb-assembly-2026:mathabhanga-002:nishith-pramanik` → age_declared NULL), while the 2,643 losers keep theirs. Any query on the §19 candidacy columns — "average declared age of winners vs losers", "graduate share among the elected" — returns NULL for 100% of winners. The existing test "a declared 2026 winner lands on the existing nomination" asserts `status='elected'` but never re-checks age/education, which is why this shipped.
  FIX:   Don't re-push a whole row to change one column: `UPDATE candidacy SET status='elected', party_version_id=?, party_raw=? WHERE id=?`, or build the upgrade row from the nomination row's own values.

[blocker / spec-conformance] packages/mandate/bin/mandate.ts:46
  CLAIM: §12's four-stage resolution pipeline does not exist: `src/ingest/resolve/` is an empty directory, so `registry:resolve` and `registry:audit` both fail, and the migrations contain no human-queue table for stage 3.
  FAILS: `npm run registry:resolve` and `npm run registry:audit` → `mandate: src/ingest/resolve/index.ts is not available yet (Cannot find module ...)`, exit 1. Of §12's four stages: (1) blocking exists but is an ascii-fold key that cannot pair scripts (separate finding), (2) scoring — no code, (3) auto-merge / auto-reject / human queue — no code *and no table*: `001_registry.sql` has `person_merge` but no candidate-pair/review-queue table, so a staffed queue has nowhere to live, (4) audit — `person_merge` has `score`, `evidence`, `decided_by`, `reverted_at` (reversible + audited, good) but 0 rows and no writer. Compounding it, ADR 0001 asserts "cycle 1 routes these to `registry:audit`" for five cross-table invariants including P5's `legal_case.stage='convicted' ⟹ source.kind='court_order'` and the party_version overlap view — none of them has a runner, so the ADR documents enforcement that does not exist. The registry ships 7,344 person rows all `review_state='unreviewed'` with known duplicates (`mandate query person "Suvendu Adhikari"` returns 3 person rows) and no path to collapse them.
  FIX:   Either land `resolve/index.ts` (block → score → queue → audited merge) plus a `person_merge_candidate` table this cycle, or amend ADR 0001 and the README to stop claiming `registry:audit` enforces the invariants, and drop the two dead npm scripts.

[major / spec-conformance] packages/mandate/src/ingest/sources/wb-static.ts:35
  CLAIM: `blockingKey` is an ascii-folding slug, so the ER blocking index is script-partitioned: a person's Bengali alias and Latin alias never share a key, which is exactly what §12 stage 1 and §19's `person_alias.norm_key` ("Indic-aware phonetic key") exist to prevent.
  FAILS: `blockingKey("Mamata Banerjee")` → `"mamatabanerjee"`; `blockingKey("মমতা ব্যানার্জী")` → `"মমতাব্যানার্জী"`. `blockingKey("Suvendu Adhikari")` → `"suvenduadhikari"` vs `blockingKey("Adhikari Suvendu")` → `"adhikarisuvendu"` — the transliteration/word-order pair the file's own comment (line 993) calls "exactly the transliteration pair the blocking index has to catch" is not caught. Consequence in the shipped index: `SELECT COUNT(*) FROM person_alias WHERE script='beng'` → 0 (no Bengali alias exists yet because `candidates.ts` has no `nameBn`), so the defect is latent on today's data but becomes live the moment a Bengali-script source lands: those persons form their own blocking island and can never be paired with their Latin counterparts, so no amount of scoring in stage 2 will ever see the pair.
  FIX:   The `ponytail:` comment already names the upgrade trigger. The load-bearing part is that `resolve/` must block on the *same* function; keep `blockingKey` the single exported entry point and swap its body when `core/indic` lands, then re-run the ingest to rewrite `norm_key`.

[major / correctness] packages/mandate/src/ingest/sources/wb-static.ts:590
  CLAIM: `party_version.id` is a positional sequence (`i + 1`) upserted by id, so when the party register shrinks, stale rows survive at high ids and a party ends up with two open-ended versions — breaking the P4 invariant the dropped Postgres EXCLUDE constraint used to guarantee. Nothing in any operational path queries the replacement view.
  FAILS: Verified: run 1 with parties [AITC, BJP, CPI(M), RSP] writes party_version 1..4; run 2 with the register regenerated as [BJP, CPI(M)] (two delisted parties dropped, neither referenced by a surviving candidacy) writes ids 1=BJP, 2=CPI(M) and leaves the stale 3=CPI(M) behind → `SELECT * FROM party_version_overlap` returns `{party_id: 'CPI(M)', a_id: 2, b_id: 3}`, two versions both `[2011-01-01, NULL)`. `asOf()` in core/temporal then returns whichever version `find()` hits first, so a party's name/symbol as-of any date is non-deterministic. `mandate ingest` exits 0 and `mandate coverage` never notices: it enumerates `sqlite_master WHERE type='table'`, which excludes the view.
  FIX:   Two independent fixes, both cheap: (a) derive `party_version.id` from content (`contentId([partyId, validFrom])`) instead of position; (b) SQLite *does* have a portable enforcement for this — a `BEFORE INSERT`/`BEFORE UPDATE` trigger with `RAISE(ABORT)` on the overlap predicate — or at minimum have the ingest fail the run when `party_version_overlap` is non-empty, which is what ADR 0001 already promises.

[major / provenance-quality] packages/mandate/src/ingest/sources/wb-static.ts:330
  CLAIM: 2,924 of the 2,932 source rows carry a `doc_hash` that is a hash of the URL string rather than of the document bytes, and a `retrieved_at` asserting a retrieval that never happened; 586 citations point at a bare site root.
  FAILS: `SELECT COUNT(*) FROM source WHERE doc_hash LIKE 'unfetched:%'` → 2924 / 2932 (99.7%): 2,921 MyNeta affidavit URLs and all 4 press URLs. Each such row also claims `retrieved_at = <the module's header date>` — e.g. the affidavit source for every 2026 candidate says `retrieved_at 2026-04-25T00:00:00Z` for a PDF whose bytes were never fetched, so §12's stated purpose for `doc_hash` ("how a re-fetch proves the document did not change") cannot be served for any of them. Separately, 586 citations resolve to `https://www.indiavotes.com/` — the site root, not a page that asserts anything: the claim "Dadhiram Ray won Mekliganj by 29,584 votes" cites a homepage. §21 F7 ("Anyone verifies a number · 10 seconds") fails for those rows. Not fabricated — the URLs, publishers and kinds are all real, and the `unfetched:` prefix is deliberately greppable — but the retrieval metadata is asserted rather than observed.
  FIX:   Make the un-retrieved state a first-class column instead of smuggling it into `doc_hash`: add `fetch_state text NOT NULL CHECK (fetch_state IN ('fetched','locator_only'))` (or make `doc_hash` nullable with a NOT NULL check keyed on fetch_state), leave `retrieved_at` as the date the *locator* was seen and say so, and have `mandate coverage` print the locator-only share so the archive backlog is visible rather than encoded in a prefix.

[major / spec-conformance] packages/mandate/src/ingest/sources/wb-static.ts:60
  CLAIM: P3 ("symbol before colour" — party identity is carried by the ECI symbol glyph) has zero data in the registry: the `symbol` table is empty and every `party_version.symbol_id` is NULL, even though half the input carries a symbol reference. The ingest's `PartyRow` type simply omits the field.
  FAILS: `src/data/parties.ts` has `symbolUrl` on 16 of its 32 parties (`/images/parties/aitc.png`, `bjp.jpg`, `inc.jpg`, …) and `color` on all 32. `PartyRow` (wb-static.ts:60-66) declares only id/name/nameBn/abbreviation/isNational, so `symbolUrl` is dropped on the floor: after the real ingest, `SELECT COUNT(*) FROM symbol` → 0 and all 63 `party_version` rows have `symbol_id IS NULL`. A cycle-2 party card following P3 has no symbol to resolve and can only fall back to colour — the precise inversion P3 forbids, and P3's cost paragraph makes the symbol set a shipping requirement, not a nice-to-have.
  FIX:   Add `symbolUrl?: string` to `PartyRow`, insert one `symbol` row per distinct value (`svg_ref` = the path, `licensed_from` = null, `allotment_kind` = null since parties.ts does not say), and set `party_version.symbol_id`. ~6 lines; the table and the FK already exist.

[major / correctness] packages/mandate/src/core/citation/index.ts:77
  CLAIM: `core/entities` and `core/citation` (≈480 lines) have no importer anywhere in the package except their own tests, and have already drifted from the schema they claim to mirror — several of the types are unsound for 100% of the rows the ingest writes.
  FAILS: Verified drifts: (1) `IngestRun.status: "running" | "ok" | "failed"` omits `'partial'`, which the DDL allows and the ingest always writes when anomalies exist — both real `ingest_run` rows have `status='partial'`, so 100% of production rows are untypeable. (2) `IngestRun.id: string` vs `id INTEGER PRIMARY KEY`. (3) `SourceKind` omits `'static_module'`, which the DDL allows and 5 source rows use. (4) `Result.postalVotes: number` / `evmVotes: number` / `voteShare: number` / `rank: number` / `margin: number` are non-nullable, but the columns are nullable and the ingest writes NULL: `SELECT COUNT(*) FROM result WHERE postal_votes IS NULL` → 4359 / 4359, so `row.postalVotes.toLocaleString()` throws for every result row. (5) `Candidacy` has no `partyRaw` although the column exists and 108 rows use it. (6) `Citation.pageNo: number | null` vs `NOT NULL DEFAULT 0`. (7) `PlaceDemographics` describes a table that no migration creates. Nothing imports any of this (`cited()`, `Cited<T>`, `Freshness`, `Source`, `Claim`, `Correction` appear only in `citation/index.test.ts`), so no compiler check catches the drift — which is why seven of them exist.
  FIX:   Ponytail: delete what has no consumer and keep what does. The types that cycle 2 will actually need can be regenerated from the DDL then, correctly; a hand-maintained mirror with no importer is exactly the "two schemas drift" failure ADR 0001 rejects for the SQL. If they stay, fix the seven divergences and have `db/index.ts` use them as the `all<T>`/`get<T>` row shapes so the compiler holds them to the schema.

[minor / spec-conformance] ops/migrations/001_registry.sql:71
  CLAIM: `place_crosswalk` is DDL only — zero rows, no writer, no reader, and no helper in `core/temporal` — so §19's "every place_version in a contest has a crosswalk to the current epoch" test can never pass and §12's stated headline use case has no code path.
  FAILS: `SELECT COUNT(*) FROM place_crosswalk` → 0 while `SELECT COUNT(DISTINCT place_version_id) FROM contest` → 294, i.e. dbt test #7 fails 294/294. `core/temporal/index.ts` exports `contains`/`asOf`/`overlaps`/`epochOf` but nothing that walks a crosswalk, so "how did this seat vote in 1999?" has no answer path even in principle. Defensible this cycle — there is exactly one epoch (`delim-2008`) and no second-epoch boundary data in the repo, so there is nothing honest to write — but it is worth stating rather than leaving the impression the table is live.
  FIX:   Either write the 294 identity rows (`from=to`, `area_share=1.0`, `method='official_order'`, `source_id=<the 2008 delimitation order>`) so the dbt test is green and the join shape is exercised, or add a `ponytail:` comment on the table naming the upgrade trigger ("first row when a second boundary_epoch lands") so a reader knows it is deliberate.

[minor / data-loss] packages/mandate/src/ingest/sources/wb-static.ts:727
  CLAIM: The declared pending-case count, education, occupation and age claims are all nested inside `if (c.affidavitUrl)`, so a candidate row without an affidavit URL loses every one of those declared facts with no anomaly recorded.
  FAILS: `affidavitUrl` is optional on `CandidateRow` (line 81) and the scraper omits it for candidates whose affidavit page it could not reach. Today all 2,920 candidates happen to have one (`SELECT COUNT(*) FROM affidavit` → 2920, `pending_cases_declared` claims → 2920), so the loss is latent. On the next scrape where 50 candidates come back without a URL, those 50 declared case counts silently vanish from the registry — no claim, no `legal_case` row, no anomaly, and `mandate ingest` reports success. That is the one number this product is most often asked for, and P5's whole point is that the count is recorded honestly rather than dropped or dressed up.
  FIX:   Hoist the `pending_cases_declared` / `age_declared` / `education_declared` / `occupation_declared` claims out of the `if`, citing `src("candidates")` (the module source, which is real and hashed) when there is no per-candidate affidavit source, and push an anomaly for the missing URL.

[minor / correctness] packages/mandate/src/ingest/sources/wb-static.ts:619
  CLAIM: Every contest is written with `lifecycle='declared'`, including the 41 contests for which no result exists — the row asserts a declaration that did not happen.
  FAILS: 1,176 contests all get `lifecycle='declared'` unconditionally, but only 1,135 have result rows (`SELECT COUNT(*) FROM result` groups to 1135 contests; the 41 gaps are already reported as `missing_result` anomalies). A cycle-2 surface that branches on `lifecycle='declared'` to decide "render the result table" will render an empty frame for those 41 seats — the empty-state P6 forbids — and an API consumer reading lifecycle as ground truth gets a false statement about the election's state.
  FIX:   `lifecycle` = `'declared'` when the seat has a result row in that election, else `'closed'` (or `'polling'`): one lookup into `seenResults`, which the code already builds at line 949.

[minor / correctness] packages/mandate/bin/mandate.ts:114
  CLAIM: `resolve --dry-run` opens a transaction around the call, which will collide with the package's only bulk-write helper — `insertMany` issues its own `BEGIN`.
  FAILS: `insertMany` (db/index.ts:24) runs `db.exec("BEGIN")`. node:sqlite rejects a nested BEGIN (verified: `cannot start a transaction within a transaction`). So `mandate resolve --dry-run` against any `resolvePersons` that writes through the package's own helper throws on the first batch; worse, `insertMany`'s catch then issues `ROLLBACK`, which discards the CLI's outer transaction, and the `finally` block's second `ROLLBACK` throws again on a connection with no open transaction — masking the original error.
  FIX:   Use a savepoint instead (`SAVEPOINT dry` / `ROLLBACK TO dry`), which nests, or give `resolvePersons` a real `dryRun` option instead of simulating one from outside.

[minor / correctness] packages/mandate/src/ingest/sources/wb-static.ts:1116
  CLAIM: The vote_share self-audit groups by `contest_id` alone, while §19 specifies the test per contest *revision* — it will report every corrected contest as broken once a revision lands.
  FAILS: `SELECT contest_id, SUM(vote_share) ... GROUP BY contest_id HAVING ABS(s - 100) > 0.5`. Equivalent today because every result row is `revision = 0`. The moment §6.10's correction ledger writes a revision-1 row for a contest — the supersession behaviour ADR 0001 D4 exists to preserve — the query sums revision 0 and revision 1 together, gets ~200, and reports the corrected contest as failing the share test forever.
  FIX:   `GROUP BY contest_id, revision`, matching the §19 wording.

[minor / spec-conformance] ops/migrations/001_registry.sql:121
  CLAIM: §12's `person_split` table is absent, so a merge decision can be reversed but a person row that was wrongly conflated at any other stage has no audited split record.
  FAILS: §12 specifies `person_split(original_id, into_ids[], decided_by, decided_at, evidence[])` alongside `person_merge`; only `person_merge` was ported (§19's DDL omits both split and `person_identifier`, and `person_identifier` *was* ported, so the omission is not a consistent "§19-only" rule). Concretely: two different humans arrive under one name from a single upstream row and are later found to be two people — the only audit primitive available is `person_merge.reverted_at`, which does not apply because no merge ever happened, so §21 F8's "a politician disputes their profile · a documented path" has no record to answer with for that case.
  FIX:   Five columns, same shape as `person_merge`. Or note the deliberate omission in ADR 0001's divergence list with its upgrade trigger, since that ADR is otherwise a complete account of what diverged from §19.

########## REVIEW 2 ##########
SUMMARY: Ran `node --test \"packages/mandate/src/**/*.test.ts\"`: 32/32 pass, 262ms. Ran the real ingest end to end: 2920 candidates + 1175 results + 294 demographics in ~470ms, producing 2932 sources, 7344 persons, 7002 candidacies, 4359 results, 22414 claims/citations, 132 anomalies, 0 uncited values.\n\nTwo blockers, both invisible to the current tests. (1) claim.id is a per-run integer sequence upserted on `id`, so the moment the input row count changes, claim N is rewritten to a new fact while the previous run's citation row survives — I reproduced a candidacy's asset claim citing a DIFFERENT candidate's affidavit, with countUncited() still returning 0, i.e. the P2 gate passes on false provenance. (2) The 2026 winner \"upgrade\" pushes a second candidacy row with null age/education and the generated upsert SETs every non-key column, so all 293 declared winners lose their affidavit-declared age and education (293/293 NULL vs 0/2643 for non-winners).\n\nBeyond those: `result.revision` is pinned to 0 so a corrected tally overwrites in place and ring 2's documented supersession never happens; person ids for the 74 duplicate-name groups depend on source array order, so a re-scrape repoints a cited id at a different human; `core/index.ts` re-exports a nonexistent `./indic/index.ts` (barrel unimportable) and `resolve/index.ts` is absent so `registry:resolve` / `registry:audit` both exit 1; `blockingKey` puts surname-first and given-name-first spellings of the same person in different buckets (48 measured groups, all plain ASCII), which will silently starve the resolver when it lands; and tsc cannot run at all (133 errors, 131 of them module-format noise from the missing `\"type\": \"module\"`).\n\nClean, verified, nothing invented: migration rollback is solid (syntax error, runtime constraint violation and a nested BEGIN all roll back fully and record nothing in schema_migration); two `ingest --fresh` runs are byte-identical after normalising timestamps (0 diff over 90k dump lines) and re-ingesting the same bytes leaves every row count unchanged; no `\"undefined\"` strings or unexpected NULLs anywhere; historical arithmetic checks out (rank-1 margin = rank1−rank2 for all 1175 rows, no contest with 0 or 2 winners, no duplicate ranks, no vote_share outside 0-100, no voters>electors — hand-verified against src/data for c0001/c0100/c0250 across three cycles); P5 holds (zero legal_case rows, no charged/convicted predicate, the bare count lands as one cited claim); Indic normalisation never throws and never produces an empty blocking bucket (max bucket 13 aliases, zero empty keys, correct on ক্ষ/জ্ঞ/ত্র conjuncts, initials-only names, single chars, and precomposed-vs-decomposed nukta which unify correctly under NFC); the ER same-contest hard negative is a real UNIQUE constraint, not a weight; and the 40 upstream duplicate (year, constituencyId) keys and 31 unresolvable party labels are all recorded verbatim with no candidacy dropped.

[blocker / correctness] packages/mandate/src/ingest/sources/wb-static.ts:463
  CLAIM: claim.id is a per-run counter (`++claimSeq`) upserted on `id`, but citation rows are keyed (claim_id, source_id, page_no). When the input row count changes, claim N is rewritten to a different fact while the PREVIOUS run's citation row survives — so a value ends up cited to a document that does not contain it, and countUncited() still reports 0.
  FAILS: Ran ingest with 1 candidate (Alpha Roy, affidavit wb26_1), then re-ran with a second candidate prepended (Beta Sen first). Result: claim 1 = `candidacy:...beta-sen / affidavit.assets.total` now has TWO citations — Beta Sen's affidavit AND Alpha Roy's (`https://myneta.info/x?candidate_id=wb26_1`). 6 of 12 claims ended up double-cited to the wrong affidavit. `countUncited(db)` = 0 and `report.uncitedValues` = 0, so `mandate ingest`'s P2 gate passes while the provenance is false. This is the ordinary workflow: `npm run scrape` regenerates candidates.ts with one more nominee, then `npm run registry:ingest` against the existing .data/registry.db. It also means every already-published claim id silently changes meaning.
  FIX:   Make claim.id content-derived like every other id in this file: `contentId([subjectRef, predicate, asOf ?? ""])` as a TEXT key (or keep INTEGER and derive it), so a claim's identity is its content and its citation cannot outlive it. Test to add: run the fixture, then re-run with one extra candidate prepended, and assert `SELECT COUNT(*) FROM (SELECT claim_id FROM citation GROUP BY claim_id HAVING COUNT(*)>1)` = 0.

[blocker / correctness] packages/mandate/src/ingest/sources/wb-static.ts:880
  CLAIM: The 2026 winner "upgrade" pushes a second candidacy row for the same id with age_declared and education_declared hard-coded null. `upsert()` SETs every non-key column, so the later row wipes the affidavit-declared age and education off all 293 declared winners.
  FAILS: src/data/candidates.ts wb26_124 "Paritosh Das" (c0012) declares age 60, education "Graduate". After `npm run registry:ingest`: `SELECT age_declared, education_declared FROM candidacy WHERE id='wb-assembly-2026:alipurduars-012:paritosh-das'` → NULL, NULL. Across the registry: 293/293 candidacies with status='elected' in wb-assembly-2026 have age_declared NULL and education_declared NULL, while 0/2643 'contesting' candidacies do. The MLA profile page for every sitting member loses its declared age and education.
  FIX:   Upgrade the status only — `UPDATE candidacy SET status='elected' WHERE id=?` — or carry the nomination's ageDeclared/educationDeclared through to the second push. Test to add: in `a declared 2026 winner lands on the existing nomination`, assert `age_declared` = 44 and `education_declared` = 'Post Graduate' from the fixture (it currently asserts only status/is_winner/margin, which is why this passed).

[major / correctness] packages/mandate/src/ingest/sources/wb-static.ts:903
  CLAIM: `revision` is hard-coded to 0 and the upsert key is (contest_id, candidacy_id, revision), so a corrected tally UPDATEs the existing row instead of appending revision 1. Ring 2's documented "append-only, immutable with supersession" invariant (002_facts.sql:5-9, entities/index.ts:335) never holds.
  FAILS: Ingested c0001/2011 with winner votes 72040 (margin 32632), then re-ingested with the ECI-corrected 72140. Registry: one row, revision=0, votes=72140, margin=32732. `SELECT COUNT(*) FROM result WHERE revision>0` = 0. The 72040 figure is gone, no `correction` row was written, so the §6.10 correction ledger has nothing to render and a citation to the old number can never be reproduced.
  FIX:   Derive revision from what is already stored: `revision = 1 + COALESCE((SELECT MAX(revision) FROM result WHERE contest_id=? AND candidacy_id=?), -1)` when the incoming votes differ from the current max-revision row; ON CONFLICT DO NOTHING otherwise so re-running identical bytes stays idempotent. Add one test that re-ingests a changed vote count and asserts two rows survive.

[major / correctness] packages/mandate/src/ingest/sources/wb-static.ts:642
  CLAIM: When two people share a name slug, whichever appears FIRST in the source array keeps the bare slug and the other gets a `-<hash>` suffix. The person id therefore depends on array order, not on the row, so reordering the scraper output reassigns a persisted, cited id to a different human.
  FAILS: Two real candidates named "Swapan Kumar Barman" exist (wb26_45/c0003 and wb26_1354/c0215). Ingesting [wb26_45, wb26_1354] gives person `swapan-kumar-barman` = the 50-year-old from c0003. Ingesting [wb26_1354, wb26_45] gives `swapan-kumar-barman` = the 61-year-old from c0215, and the other becomes `swapan-kumar-barman-288547`. 74 such collision groups (164 candidate rows) exist in candidates.ts today. `npm run scrape` regenerates that file with no order guarantee, so a re-scrape silently repoints every claim, candidacy and citation on `person:swapan-kumar-barman` at a different person. (Two back-to-back `ingest --fresh` runs over unchanged bytes ARE byte-identical — I verified 0 diff after normalising timestamps — the instability is to reordering only.)
  FIX:   Always disambiguate from the row's own stable key rather than from position: `id = takenPersonIds.has(base) || <base is known-ambiguous> ? \`${base}-${contentId([scopeKey]).slice(0,6)}\` : base` computed after a pre-pass that counts base slugs, so no id depends on which row was seen first.

[major / correctness] packages/mandate/src/core/index.ts:5
  CLAIM: `core/index.ts` re-exports `./indic/index.ts`, which does not exist. The documented core entry point cannot be imported at all, at runtime or by tsc.
  FAILS: `node --experimental-strip-types -e "await import('packages/mandate/src/core/index.ts')"` → `ERR_MODULE_NOT_FOUND: Cannot find module '.../src/core/indic/index.ts'`. `tsc -p packages/mandate/tsconfig.json` → `error TS2307`. It is latent only because wb-static.ts deliberately routes around it (see its GAP comment at line 24) and imports `../../core/ids.ts` directly; the first file that imports the barrel as README.md advertises dies on load.
  FIX:   Drop line 5 until `core/indic/index.ts` lands (one-line deletion), or add the module. Same class of problem: `bin/mandate.ts:49` imports `../src/ingest/resolve/index.ts`, which is also absent, so `npm run registry:resolve` and `npm run registry:audit` both exit 1 with "is not available yet" — 2 of the 5 registry scripts are non-functional and there is no entity-resolution code in the tree.

[major / correctness] packages/mandate/src/ingest/sources/wb-static.ts:35
  CLAIM: `blockingKey` = slug with hyphens stripped, so it is order-sensitive. The dominant duplicate pattern in this data — Lokdhaba writes SURNAME FIRST, MyNeta writes given-name first — lands in different blocking buckets, meaning a resolver keyed on norm_key (the only index the DDL provides, 001_registry.sql:107) will never even compare the pairs it exists to catch.
  FAILS: Measured over the 7344 real person_alias rows: 48 groups of token-identical names sit in more than one norm_key bucket. Concretely `ADHIKARY PARESH CHANDRA` (2011 winner, c0001) → key `adhikarypareshchandra`, `PARESH CHANDRA ADHIKARY` (2016 runner-up, same seat, same human) → key `pareshchandraadhikary`. Others: `agarwalkanaialal`~`kanaialalagarwal`, `mostaquealam`~`alammostaque`, `islammdnajrul`~`mdnajrulislam`. All plain ASCII — this is not the transliteration limitation the ponytail comment at line 26 documents. Bucket health is otherwise fine (largest bucket 13, zero empty keys, no throw on '', '.', 'A. K. Roy', single chars, ক্ষ/জ্ঞ/ত্র conjuncts, and precomposed vs decomposed nukta correctly unify under NFC).
  FIX:   Sort the tokens: `slug(name).split("-").sort().join("")`. One line, keeps determinism, collapses all 48 groups into one bucket each. Recording the ordered key as a second alias row would keep the exact-order signal for scoring.

[major / test-coverage] packages/mandate/tsconfig.json:1
  CLAIM: The typecheck cannot run: there is no `"type": "module"` reachable from packages/mandate, so tsc reports 133 errors of which 131 are TS1295/TS1287 module-format noise. The strict-TS constraints the package is built against are unverified, and the 2 genuine errors are invisible in the flood.
  FAILS: `node_modules/.bin/tsc -p packages/mandate/tsconfig.json` → 133 errors. Filtering out TS1295/TS1287 leaves the two real ones (the missing indic/ and resolve/ modules), plus `src/db/migrate.ts:18` `.map((r) => String(r.filename))` → TS7006 implicit any, plus TS1470 on both `import.meta` uses. Nothing in `npm test` or the CLI exercises the type layer, so a future `noUncheckedIndexedAccess` violation ships silently.
  FIX:   Add `packages/mandate/package.json` containing `{"type":"module"}` (the package dir is in scope; root package.json stays untouched) and a `typecheck` script. Then fix the 4 remaining real errors.

[minor / correctness] packages/mandate/src/ingest/sources/wb-static.ts:946
  CLAIM: The `zero_vote_result` anomaly names only the vote_share dbt test, but the 293 zero-vote 2026 rows also break §19's "winner margin equals rank-1 minus rank-2 votes" (04-engineering.md:489): they assert a margin with no tallies behind it.
  FAILS: c0012/2026 writes result row votes=0, vote_share=0, rank=1, is_winner=1, margin=70420. Any consumer computing winner-vs-runner-up from `votes` gets 0 while `margin` says 70420. All 293 rows with votes=0 are the only rows in their contest, so the arithmetic invariant cannot be checked at all — and the anomaly text does not say so. (The 1175 historical rows are clean: I verified rank-1 margin = rank1.votes − rank2.votes for every one, 0 contests with 0 or 2 winners, 0 duplicate ranks, and hand-checked c0001/c0100/c0250 across 2011/2016/2021 against src/data/historical-results.ts.)
  FIX:   Add the margin test to the anomaly detail, or leave `margin` NULL when `votes` is 0 and keep the declared figure as a `margin_votes` claim on the contest — an asserted margin with no tallies is exactly the kind of number P2 says must be attributable.

[minor / correctness] packages/mandate/bin/mandate.ts:200
  CLAIM: `mandate coverage` against an existing-but-unmigrated database throws a raw ERR_SQLITE_ERROR stack trace, contradicting the file's own header promise that "nothing throws a stack trace at an operator".
  FAILS: In a fresh directory with an empty `.data/registry.db` (which `open()` creates on demand), `node packages/mandate/bin/mandate.ts coverage` prints `Error: no such table: claim` plus a six-frame stack from src/db/index.ts:11. The table loop above it returns zero rows, so the operator gets no hint that the fix is `mandate migrate`.
  FIX:   Guard once: if `SELECT COUNT(*) FROM sqlite_master WHERE name='claim'` is 0, `fail("registry not migrated — run: mandate migrate")`.

[minor / docs-accuracy] docs/adr/0002-single-mandate-package.md:39
  CLAIM: ADR 0002 records as a KEPT boundary that "nothing in `packages/mandate` imports from `src/` (the old Next app) … checkable with one grep". The grep fails: wb-static.ts imports src/data nine times.
  FAILS: `grep -rn 'src/data/' packages/mandate --include='*.ts'` returns wb-static.ts lines 180, 199-206 — a `fileURLToPath` into `../../../../../src/data/` plus eight dynamic `import()`s. Anyone applying the ADR's stated reversal condition ("if the one-way dependency direction is violated twice, split early") acts on a false premise, and the ADR misdescribes the actual seam.
  FIX:   Restate the invariant to what is true and intended: the Next app does not import from packages/mandate, and mandate's only reach into src/ is the read-only `src/data/*.ts` ingest source, which disappears when real scrapers land.

[minor / correctness] packages/mandate/src/core/citation/index.ts:9
  CLAIM: `SourceKind` omits `'static_module'`, which 001_registry.sql:25 explicitly allows and the ingest writes. The type cannot describe rows the pipeline actually creates.
  FAILS: `SELECT COUNT(*) FROM source WHERE kind='static_module'` = 5 after a real ingest (constituencies, parties, currentMLAs, cabinet, mps). Typing any of those rows as `Source` is a compile error, so the first API handler that reads `source` will need a cast or will widen the type by hand.
  FIX:   Add `| "static_module"` to the union, with the same comment the DDL carries.

########## REVIEW 3 ##########
SUMMARY: Reviewed packages/mandate/** (11 source files, ~2.4k lines), ops/migrations/*.sql, docs/adr/**, and the package.json script diff. Ran `npm test` (32/32 pass), the full CLI (`migrate`, `ingest --fresh`, `ingest`, `resolve`, `audit`, `query`, `coverage`), and ~40 verification queries against the produced .data/registry.db. Every finding below was reproduced by running code; nothing is modified (git status unchanged).\n\nClean categories, stated rather than invented: ZERO dependencies added — the package.json diff is six script lines and nothing else, and the code uses only node:sqlite / node:crypto / node:fs / node:path / node:url / node:test. No node builtin is reimplemented (sha256 comes from node:crypto, migration dir from fileURLToPath+URL). No classes, no factories, no strategy objects, no enums/namespaces/parameter properties, no options object for a value that never varies. Bulk inserts ARE transactional (db/index.ts:insertMany, with a rollback test). No duplicated helper across the two build agents — only one slug and one blockingKey exist, and `slug()` correctly does NOT reuse src/lib/utils.ts:slugify, which would delete Bengali entirely. The §19 port is faithful with divergences documented in ADR 0001; PRAGMA foreign_keys is on and `PRAGMA foreign_key_check` is empty. On real data the dbt invariants that can hold do hold: winner margin == rank1−rank2 on all 842 contests with a rank-2 row, zero contests with two winners, zero duplicate ranks, zero cross-epoch contests, 0 uncited values, 2920/2920 candidacies kept with 31 unresolved party labels recorded (CPI and NCP genuinely are absent from parties.ts), 0 legal_case rows from criminalCases, and the census vintage lands as as_of=2011-01-01. The empty `place.names` is correct, not a bug — constituencies.ts's nameBn/districtBn are Latin copies and jsonNames rightly drops them.\n\nTwo blockers: the core barrel is unimportable (re-exports a directory that was never written), and claim ids are positional so re-ingesting changed data attaches one candidate's affidavit to another candidate's numbers — a P2 violation the ingest's own uncited-value gate cannot see. Then four majors (no failure path around the writes, unvalidated URLs aborting the run, 380 lines of already-drifted unused types, and an ADR promising an audit gate that does not exist) and eleven minors. The largest deletion win is core/entities/index.ts plus the unused half of core/citation and all of core/temporal: ~450 lines with zero importers. One structural note not filed as a finding: `ingest()` is a single ~900-line function holding 16 row accumulators in one scope; it works and is well commented, but it is the one place where the next change will be hard.

[blocker / correctness] packages/mandate/src/core/index.ts:5
  CLAIM: The core barrel re-exports `./indic/index.ts`, which does not exist (packages/mandate/src/core/indic/ is an empty directory), so the package's documented public entry point cannot be imported at all.
  FAILS: `node -e "import('packages/mandate/src/core/index.ts')"` -> `ERR_MODULE_NOT_FOUND: Cannot find module .../core/indic/index.ts`. Verified by running it. `tsc` also reports TS2307 on this line. No test imports the barrel, so `npm test` is 32/32 green and the breakage is invisible; wb-static.ts:24-32 even documents the workaround (importing core/ids.ts directly) instead of fixing the barrel. Any cycle-2 consumer (`resolve/`, apps/reader) that writes `import { slug } from "../core/index.ts"` crashes on the first import.
  FIX:   Delete line 5 until core/indic/ ships (its only consumer, `blockingKey`, already lives in wb-static.ts), and add one test that imports the barrel so this can never regress.

[blocker / provenance] packages/mandate/src/ingest/sources/wb-static.ts:463
  CLAIM: claim.id is a positional counter (`++claimSeq`) used as the upsert key, while citations are keyed (claim_id, source_id, page_no) and nothing is ever deleted — so re-ingesting changed data rebinds a claim id to a different fact while the previous run's citation survives, making the registry assert that a document supports a value it does not contain.
  FAILS: Verified end-to-end: ingest a bundle with candidate `wb26_1` (Hiten Barman), then ingest again with one candidate prepended (exactly what `npm run scrape:2026 && npm run registry:ingest` produces, since registry:ingest does not pass --fresh). Result: claim 1 = `candidacy:...:new-person / affidavit.assets.total = 5` now carries TWO citations — `https://myneta.info/x?candidate_id=9` AND `https://myneta.info/x?candidate_id=1`, i.e. New Person's asset figure cites Hiten Barman's affidavit. Same for `pending_cases_declared` and `age_declared`. `countUncited()` still returns 0, so the P2 gate in bin/mandate.ts:102 passes and nothing notices.
  FIX:   Make the claim key content-derived like every other id — `contentId([subjectRef, predicate, asOf])` as a TEXT primary key (or add UNIQUE (subject_ref, predicate, as_of) and look the id up) — or `DELETE FROM citation WHERE claim_id IN (...)` before writing the batch.

[major / error-handling] packages/mandate/src/ingest/sources/wb-static.ts:1148
  CLAIM: There is no error path around the 20 per-table `insertMany` calls: a failing write propagates out of `ingest()`, so the `ingest_run` row stays `status='running'` with `finished_at` NULL forever, earlier tables stay committed, and bin/mandate.ts:76 has no try/catch so the operator gets a raw stack trace.
  FAILS: Verified: feed a bundle whose two constituencies share `assemblyNumber: 1` (one bad row in constituencies.ts). The `contest` insert throws `UNIQUE constraint failed: contest.election_id, contest.place_version_id`; afterwards `place` holds 3 committed rows, `contest` holds 0, and `SELECT id,status,finished_at FROM ingest_run` returns `{id:1, status:'running', finished_at:null}`. The DDL's `status` CHECK includes 'failed' and it is never written, so a failed run is indistinguishable from one still in flight. The file header at line 2-3 and bin/mandate.ts:3 both promise "nothing throws a stack trace at an operator".
  FIX:   Wrap the write block in try/catch: on error `UPDATE ingest_run SET status='failed', finished_at=?, anomalies=?` then rethrow; catch at the top of bin/mandate.ts and route through `fail()`.

[major / validation] packages/mandate/src/ingest/sources/wb-static.ts:426
  CLAIM: `urlSource()` calls `new URL(url).hostname` on unvalidated per-row `sourceUrl` values from current-mla.ts / cabinet.ts / wbmps.ts — a trust boundary with no validation, in a pipeline that carefully records anomalies for every other bad input.
  FAILS: Verified: set `currentMLAs[0].sourceUrl = "not a url"` and the whole ingest dies with `TypeError: Invalid URL` before a single row is written, leaving `ingest_run` at status='running'. One malformed URL in a 294-row hand-curated file destroys the run, while an unknown constituencyId or an unresolvable party in the same run is merely recorded.
  FIX:   `URL.parse(url)` (Node 22+, returns null instead of throwing) — on null push an `{kind:"bad_source_url"}` anomaly and fall back to publisher `null`, matching how every other bad input is handled.

[major / over-engineering] packages/mandate/src/core/entities/index.ts:1
  CLAIM: 380 lines of row types with zero importers anywhere in the repo — the ingest declares its own local row shapes and writes `Param[][]` arrays — and they have already drifted from the DDL they claim to mirror, so they are worse than absent: they will typecheck a wrong row.
  FAILS: Verified drift, all six concrete: (a) `SourceKind` (core/citation/index.ts:9-21) omits `'static_module'`, which the DDL CHECK allows and the ingest writes 5 rows of, so `const s: Source = { kind: "static_module" }` is a type error on the real data; (b) `IngestRun.status` (citation:77) omits `'partial'`, the value the ingest actually writes and the DDL CHECK actually allows; (c) `Person` has no `canonicalNameScript` though the column exists and is written; (d) `PersonAlias` has no `firstSeen`; (e) `Candidacy` has no `partyRaw` — the one column the unresolved-party requirement depends on; (f) `PlaceDemographics` describes a `place_demographics` table that exists in no migration (grep returns nothing). Nothing imports any of these, so nothing catches it.
  FIX:   Delete core/entities/index.ts and every unused type in core/citation/index.ts (keep `Anomaly`, the only one imported); re-add a type when a real reader — an API route or a resolver — needs it, generated from or tested against the DDL.

[major / docs-drift] docs/adr/0001-sqlite-dev-postgres-prod.md:80
  CLAIM: The ADR states "cycle 1 routes these to `registry:audit`" for five cross-table invariants the schema cannot express, and line 56 says "registry:audit is expected to fail the run when the view returns rows" — but `registry:audit` is unimplemented, so not one of those invariants is ever checked against the real database.
  FAILS: `npm run registry:audit` -> `mandate: src/ingest/resolve/index.ts is not available yet (Cannot find module ...)`, verified. Consequently: insert two overlapping `party_version` rows for one party and nothing in the repo notices (the `party_version_overlap` view is only ever queried by migrate.test.ts against synthetic rows, never against .data/registry.db); same for `legal_case.stage='convicted' => source.kind='court_order'`, `booth_result <= result`, and the crosswalk-to-current-epoch test. Only the vote_share and uncited-value checks are actually run (inline in the ingest).
  FIX:   Either implement `mandate audit` as a plain SQL invariant runner over the five queries (it needs no entity resolution — that is a separate subcommand), or change the ADR to say the invariants are unenforced in cycle 1 and name the cycle that adds them.

[minor / dead-code] packages/mandate/bin/mandate.ts:106
  CLAIM: ~55 lines of CLI scaffolding (`resolveModule()` at 46-55, `case "resolve"` at 106-124, `case "audit"` at 126-138, plus two `as (d: unknown, o: unknown) => unknown` casts and two `typeof === "function"` guards) exist for a module that does not exist, and the --dry-run path is broken by construction.
  FAILS: `npm run registry:resolve` and `npm run registry:audit` both print `mandate: src/ingest/resolve/index.ts is not available yet` — verified. Worse, when resolve/ does land: line 114 does `db.exec("BEGIN")` and then calls into code that will use `insertMany`, which opens its own `BEGIN` — SQLite raises `cannot start a transaction within a transaction`, so `--dry-run` fails the moment it has anything to roll back.
  FIX:   Delete both branches and `resolveModule()`; add them back in the same commit that adds resolve/index.ts, with the dry-run implemented as an explicit option on `resolvePersons` rather than an outer transaction.

[minor / dead-code] packages/mandate/src/core/temporal/index.ts:19
  CLAIM: `contains`, `asOf`, `overlaps`, `epochOf` and `cited()`/`Cited<T>` have zero production callers — grep shows their only references are their own test files — and the one temporal invariant the registry actually relies on is implemented a second time in SQL as the `party_version_overlap` view.
  FAILS: Concretely: `overlaps()` in core/temporal is the half-open window check, and `party_version_overlap` in 001_registry.sql:168-175 is the same predicate in SQL with a third copy as a local `overlaps(db)` helper in migrate.test.ts:136. Two implementations of one invariant, only the SQL one wired to data; the TS one can silently disagree (e.g. someone changes the COALESCE sentinel) and no test compares them.
  FIX:   Delete core/temporal's four functions and `cited()` until a caller exists (the ingest passes ISO strings straight through); keep the `ISODate`/`ISOTimestamp` aliases if you want the documentation value.

[minor / correctness] packages/mandate/src/ingest/sources/wb-static.ts:335
  CLAIM: `moduleDate(k)` pushes a `no_header_date` anomaly every time it is called, and five modules are dated twice (once in the loop at 405, again at 674/785/963/995/1018), so a dateless file yields duplicate anomalies.
  FAILS: Verified: with `docs.candidates.retrievedOn = null`, the report contains two byte-identical `{kind:"no_header_date", ref:"module:candidates"}` entries. Anomaly counts are what the audit report and `ingest_run.anomalies` publish, so the operator-visible count is inflated and de-duping it becomes the reader's problem.
  FIX:   Memoise: `const moduleDay = new Map<ModuleKey,string>()` and return the cached value, pushing the anomaly only on first computation.

[minor / over-engineering] packages/mandate/src/ingest/sources/wb-static.ts:1196
  CLAIM: A check and a published metric that cannot ever fail: `countUncited`'s third term counts `result WHERE source_id IS NULL OR source_id = ''`, but 002_facts.sql:25 declares `source_id TEXT NOT NULL` and every id is 16 hex chars from `contentId`, so the term is always 0; bin/mandate.ts:206-208 reports "results sourced" as `source_id IS NOT NULL`, which is pinned at 100.0% by the same constraint.
  FAILS: On the real database `coverage` prints `results sourced 4359/4359 100.0%` no matter what the ingest does — a provenance metric that reads perfect by definition is worse than no metric, because it is the number someone will cite as evidence. The real gap it should be measuring (2924 of 2932 sources carry a `unfetched:` placeholder doc_hash, i.e. the bytes were never fetched) is reported nowhere in `coverage`.
  FIX:   Drop the `result` term from `countUncited` and replace the "results sourced" row with something falsifiable, e.g. `results whose source_id resolves to a source with a real doc_hash`.

[minor / over-engineering] packages/mandate/src/ingest/sources/wb-static.ts:186
  CLAIM: The `dir` parameter on `readModuleDoc`/`loadStaticBundle` is passed by no production caller and is incoherent by construction — `loadStaticBundle(dir)` hashes the eight files under `dir` but always `import()`s them from a hardcoded `../../../../../src/data/` — and it joins paths by string concatenation instead of `node:path`.
  FAILS: `loadStaticBundle("/tmp/snapshot/")` returns `docs` hashed from /tmp/snapshot while `candidates` etc. come from src/data — provenance hashes silently attributed to bytes that produced none of the rows. And `readModuleDoc("parties.ts", "/tmp")` (no trailing slash) reads `/tmpparties.ts` -> ENOENT, because line 186 is `readFileSync(dir + file)`.
  FIX:   Drop both `dir` parameters (tests inject a whole `bundle` already) and use `join(DATA_DIR, file)` for the one remaining read.

[minor / test-coverage] packages/mandate/bin/mandate.ts:19
  CLAIM: bin/mandate.ts has no test at all, and its flag parser silently ignores `--flag=value` forms for boolean flags because `has()` does an exact `argv.includes("--fresh")`.
  FAILS: Verified: `node packages/mandate/bin/mandate.ts ingest --fresh=1` prints no "dropped" line and leaves .data/registry.db in place (mtime unchanged path, size grew) — the operator asked for a clean rebuild, got an incremental upsert onto the old database, and given the claim-id defect above that is exactly the path that produces false citations. No test exercises `has`, `num`, or any subcommand.
  FIX:   Make `has()` match `--name` or `--name=true|1`, or reject unknown `--x=y` forms with `fail()`; add one test for the argv helpers (extract them into src/ so they are importable).

[minor / correctness] packages/mandate/src/ingest/sources/wb-static.ts:727
  CLAIM: Every declared claim for a candidate — pending case count, age, education, occupation and all four affidavit money fields — is nested inside `if (c.affidavitUrl)`, so a candidate row without that URL loses all of them with no anomaly recorded.
  FAILS: Today 0 of 2920 candidates lack `affidavitUrl`, so this is latent, but MyNeta rows without an affidavit link are routine. Add one candidate `{criminalCases: 7, affidavitUrl: undefined}` and the registry holds a candidacy with no `pending_cases_declared` claim, no anomaly, and `uncitedValues` still 0 — the declared case count silently vanishes, which is the exact failure the P5 section 20 lines above exists to prevent.
  FIX:   Move the four `person:` claims out of the `if` and cite them to `src("candidates")` (the module row) when there is no per-candidate affidavit source; push a `{kind:"missing_affidavit_url"}` anomaly.

[minor / simplification] packages/mandate/src/ingest/sources/wb-static.ts:153
  CLAIM: Three parallel structures keyed by the same `ModuleKey` — `MODULE_KEYS` (141), `MODULE_FILES` (153), `MODULE_META` (348-400) — plus a fourth implicit mapping in `loadStaticBundle`'s positional `m[0..7]` array.
  FAILS: Adding a ninth module requires four coordinated edits, and the positional array is the fragile one: inserting an import at position 2 of the `Promise.all` at 198-207 without renumbering `m[2]..m[7]` at 213-220 assigns `parties` rows to the `candidates` field. Nothing type-checks that alignment because every element is cast (`m[2].candidates as readonly CandidateRow[]`).
  FIX:   One array of `{ key, file, kind, publisher, title, licence, load: () => import(...), pick: (m) => m.x }` records and derive `MODULE_KEYS` from it; the positional array disappears.

[minor / over-engineering] packages/mandate/src/ingest/sources/wb-static.ts:265
  CLAIM: `scriptOf` hand-rolls Unicode block ranges (`/[ঀ-৿]/`, `/[ऀ-ॿ]/`) where the platform has script property escapes, and the hand-rolled Devanagari range is narrower than the script.
  FAILS: A name using Devanagari Extended or Vedic Extensions (U+A8E0–U+A8FF, U+1CD0–U+1CFF) — legal in Devanagari text — falls through to `"latn"`, and `person_alias.script` records the wrong script for the ER blocking index. `/\p{Script=Devanagari}/u` covers them; `/\p{Script=Bengali}/u` covers Bengali including future additions.
  FIX:   `const scriptOf = (s: string) => /\p{Script=Bengali}/u.test(s) ? "beng" : /\p{Script=Devanagari}/u.test(s) ? "deva" : "latn";`

[minor / tooling] packages/mandate/tsconfig.json:1
  CLAIM: The package cannot be type-checked, so none of the stated TypeScript constraints are actually enforced and the two genuine type errors are buried in noise.
  FAILS: `npx tsc -p packages/mandate/tsconfig.json --noEmit` emits 133 errors: ~120 are TS1295/TS1287 caused solely by there being no `"type": "module"` in the nearest package.json, plus `node:sqlite` missing from @types/node@20, plus src/data/*.ts pulled in via the dynamic imports failing on the Next-only `@/types` alias. The two real ones — `TS2307 Cannot find module './indic/index.ts'` and `TS2307 '../src/ingest/resolve/index.ts'` — are indistinguishable from the noise, which is how the broken barrel (blocker 1) shipped green.
  FIX:   Add `packages/mandate/package.json` = `{"type":"module"}` (a new file inside the allowed scope, also silences the MODULE_TYPELESS_PACKAGE_JSON warning on every command) and `"exclude": ["../../src/**"]`; the remaining node:sqlite gap needs a @types/node bump, so note it in the README until then.

[minor / correctness] packages/mandate/src/ingest/sources/wb-static.ts:878
  CLAIM: A declared 2026 contestant that matches an existing nomination only has its status upgraded when it is the winner (`if (match && isWinner)`), so an identical declared fact yields `defeated` or `contesting` depending on whether the name happened to match.
  FAILS: 293 of the 2026 rows currently carry a winner only, so this is latent. Add a `runnerUp` to one 2026 row whose name matches a nomination: the registry stores a `result` row with rank 2 and `is_winner=0` while the candidacy still says `status='contesting'`; the same runner-up under a spelling that does not match a nomination gets `status='defeated'`. Any query for "seats this person lost" returns different answers for the two.
  FIX:   Push the upgrade row whenever `match` is truthy, with `status = isWinner ? "elected" : "defeated"`.


== total review objects: 3 ==
